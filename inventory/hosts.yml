---
# La configuration s'applique toujours à la machine locale : pas de SSH,
# pas d'inventaire distant. Le poste courant est le seul hôte géré.
all:
  children:
    workstation:
      hosts:
        localhost:
          ansible_connection: local
          ansible_python_interpreter: "{{ ansible_playbook_python }}"
